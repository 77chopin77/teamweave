spring:
  datasource:
    url: jdbc:h2:mem:testdb;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DEFAULT_NULL_ORDERING=HIGH
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: none   # or update でもOK。Flyway使わないなら none 推奨
  flyway:
    enabled: false

# テストでも JwtProvider が起動するので鍵が必要
jwt:
  secret: "pPr3U4f0w1nIMyD2hE1G9zQ8lV6rX0yJ7bW2cM5tA9kQ4sN1eR8vL2pF6dH0uK3aZ7q"
  expiration-seconds: 3600
